Dir.glob('lib/tasks/*.rake').each { |r| load r} # Load all rake tasks in the lib/tasks directory

require 'rubygems'
require 'date'
require 'bundler'
require './api'
require './api-eb'
require './launchpad'
require './offline'

Bundler.setup

require './environment'